# Edge case evidence

Generated by `scripts/edge-case-test.sh --evidence` on 2026-09-16T22:12:42Z.

Boundaries verified through the full stack — HTTP, JSON, JDBC, PostgreSQL — rather than
in isolation. A scale lost in a JSON number or a timezone applied by a driver is
invisible to a unit test.

| Case | Expected | Actual | Why it matters |
| --- | --- | --- | --- |
| sub-cent price rounds once, at scale 4 | `0.0233` | `0.0233` | rounding the unit price first, or rounding twice, gives a different figure |
| a 6-decimal quantity survives JSON and NUMERIC(19,6) | `1.123456` | `1.123456` | a scale silently truncated in transit bills the wrong amount |
| quantity of exactly zero is rejected | `REJECTED:422` | `REJECTED:422` | zero is not billable usage, and a zero-amount charge is noise on an invoice |
| a negative quantity is rejected | `REJECTED:422` | `REJECTED:422` | it would produce a negative charge — a credit note by accident |
| a malformed event id is rejected | `REJECTED:422` | `REJECTED:422` | the event id is the idempotency key; an unparseable one cannot deduplicate |
| a non-ISO timestamp is rejected | `REJECTED:422` | `REJECTED:422` | occurredAt selects both the pricing rule and the period — a guess would misbill |
| a far-future timestamp is rejected | `REJECTED:422` | `REJECTED:422` | it would land in a period that has not started |
| ordinary clock skew is tolerated | `ACCEPTED:202` | `ACCEPTED:202` | rejecting a two-minute skew would fail healthy integrations |
| an almost-empty body is rejected, not a 500 | `REJECTED:422` | `REJECTED:422` | missing fields are the caller's error, and the response must say which |
| malformed JSON is a 400, not a 500 | `400:400` | `400:400` | a parse failure is a client error and must not look like a service fault |
| an event on the old side of a price change is accepted | `ACCEPTED:202` | `ACCEPTED:202` | the changeover instant must not reject either side |
| an event on the new side is accepted | `ACCEPTED:202` | `ACCEPTED:202` | start-inclusive means the changeover instant itself belongs to the new rule |
| usage with no pricing rule is accepted, not rejected | `ACCEPTED:202` | `ACCEPTED:202` | the rule may be created tomorrow; the usage is real either way |
| unrated usage waits rather than billing zero | `>=1` | `1` | billing zero would hide a configuration gap behind a plausible invoice |
| a years-old event is accepted at the door | `ACCEPTED:202` | `ACCEPTED:202` | the decision belongs to rating, which has the context to quarantine it |
| a body naming another tenant is rejected | `REJECTED:422` | `REJECTED:422` | the header is identity; trusting the body would let anyone bill anyone |
| no tenant header at all is refused | `400` | `400` | unscoped writes must not reach the database |
| a blank tenant header is treated as absent | `400` | `400` | whitespace must not resolve to a tenant named ' ' |
| a period with no usage totals zero | `0.0` | `0.0` | an empty period must be reportable, not an error or a gap |
| an empty period still balances | `True` | `True` | 0 = 0 + 0 + 0 is a valid reconciliation |
| a malformed period is a client error, not a 500 | `400` | `400` | bad input must be reported as bad input |
| an unknown pricing rule is a 404 | `404` | `404` | and is indistinguishable from another tenant's rule, which is the point |

**All cases passed.** Run completed at 2026-09-16T22:12:47Z.
